[profile.default]
fail-fast = false
retries = 0
slow-timeout = { period = "60s", terminate-after = 2 }

[profile.ci]
fail-fast = false
retries = { count = 1, backoff = "fixed", delay = "1s" }
slow-timeout = { period = "60s", terminate-after = 2 }
